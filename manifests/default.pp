include chocolatey

package { 'notepadplusplus':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'googlechrome':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}


package { 'firefox':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'git':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'python3':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'nodejs':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'putty':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'dotnetcore-sdk':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'vscode':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'awscli':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'golang':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'conemu':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'azure-cli':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'keepass':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'jdk11':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}

package { 'jetbrainstoolbox':
	ensure		=> latest,
	provider 	=> 'chocolatey',
}
